struct JoinLeaveChannelMessage {
    command: String
    channel: String
}

struct PrivMessage {
    peer: String
    payload: String
}

struct ChannelMessage {
    channel: String
    sender: String
    payload: String
}