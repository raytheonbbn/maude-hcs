type Message {
    uuid: string;
    content: string;
    timestamp: datetime;
}

const MESSAGE_INTERVAL = 1000; // Interval in milliseconds for sending messages

function createMessage(id: int, content: string): Message {
    return {
        id: id,
        content: content,
        timestamp: now()
    };
}