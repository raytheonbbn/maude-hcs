# Directory structure 

* `results-final/` directory containing the statistical guarantees produced by SMC for cp1 demo inputs (these use cases configurations are located under `../use-cases/challenge-problem-1`)
   * `cp1demo-rawdata/` directory containing the raw samples used by SMC to produce the statistical guarantees

* `pwnd_cp1_data/` directory containing the raw samples produced by the T&E team on the testbed

