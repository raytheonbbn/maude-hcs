
um -> mtg-eq -> cl-eq  -obs> server-eq
   <-        <-      <-    <-
   
mtg sends requests to client
client sends to obs / server proxy
obs logs req and forwards to server
server responds to obs, obs logs if responseLog true
forwards to client
eventuall client responds to mtg 


rl > eq
also add rCtr(j) and replace delayMsgs(msgs,null) by delayMsgsX(j,msgs,null)  -  delayMsg requires a rewrite position
requires eventually adding sampleUniformX to sampler-x
requires replacing delayParamd by delayParamdX that uses the rCtr index

update
 ua-actor-eq --- computes delay directly
 masTGen-actor-eq  #
 mastodon-eq #
 masObs-eq #

mkUMActor >> mkUMActorEq
mkMasTGenAttrs >> mkMasTGenAttrsEq 
mkMasTGenActor >> mkMasTGenActorEq
makeMastodonClient >> makeMastodonClientEq
makeMastodonServer >> makeMastodonServerEq

FIX DNS constructors and scenario

dnsTgen should use uniqueid


tgen parameters

start time and random index
actor index
mamodel index

addtoSent adds 3 copies of the 'mastodon query


Moved
op ma2HashstrMap : MAModel -> Map{Nat,String} .
op ma2HashstrLen : MAModel -> Nat .
op jhashstrl2HashstrMap : JVL Nat Map{Nat,String} -> 
                           Map{Nat,String} .
and eqns
from CONTENT-MANAGER in cm-aux
to common/maude/arkov-action-model.maude

Should be transparent as must inc MARKOV-ACTION-MODEL 
to use


        