CLT updates to IRC

I tried to mark changes with **** CLT

Added test_prob_irc-clt.maude --- a version of
test_prob_irc.maude with updates to actor construction.
The latter may be broken.

Fixed the name sort confusion and a few other small bugs.  

Added a mechanism for the IRC server to broadcast
to client tunnel endpoints
1.   sorts ClientAddress ClientAddressMap  and helper functions
2. attribute clientAddressMap: ClientAddressMap -> Attribute [ctor] .
3. modified in common/_aux
   op $createIrcMsg : ClientAddressMap --- Address 
  Address IrcUserName IrcMsg IrcUserNameList -> Config .
to lookup up connection address of DST_USER in the map.
Fortunately all broadcast calls reduce to createIrcMsg which calls $ version,
  
4. irc tunnel endpoint entered in map at join time
   and map is cleared when client leaves
5. rule for handling Ping TimeOut 
        getRaceboatAddr(attrs) 
  replaced by
        getClientAddr(getClientAddresMap(attrs),
                      makeIrcUserName(IRC_CLIENT_ADDR))
TODO currently join messages are just in the initial
configuration.  If they can be sent by the irc Client
will need to fix the rb receiver CM at the IRC server
to send messages from the rb sender CM (or we need
another acter that deals with this and with marshalling
and unmarshalling).

In the server pong timeout rule, a check was added so that quit messages are only sent if user stale is true.

The function that creates messages to forward to
channel members originally put the sending user name as
the first (destination?) parameter.  That was changed
use the channel name instead.  

TODO The PrivMsgReq/Rsp allow the first argument to be
an Irc username as well as a channel name.  
The IRC Server currently can't handle this case.
A clause has been added to getSize for this case,
but it probably is wrong as it guesses the address size.


TODO
I propose using rCtr and delayMsgsX (need to commit this somewhere) in probablistic rules to reduce use of counter/rand which adds  2-3 rewrites per  message
delivery rule.   Also makes it easier to put sampling expressions where we want them.


