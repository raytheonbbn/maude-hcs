[
    {
        "name": "only_obfs_maude",
        "desc": "test that all messages are received on isolated obfs channel",
        "expected": "true",
        "build_cfg": "only_obfs", 
        "arg": "all-msgs-received(initConfig) and non-zero-msgs-received(initConfig)"
    },

    {
        "name": "only_skyhook_maude",
        "desc": "test that all messages are received on isolated skyhook channel",
        "expected": "true",
        "build_cfg": "only_skyhook", 
        "arg": "all-msgs-received(initConfig) and non-zero-msgs-received(initConfig)"
    },
    
    {
        "name": "only_mastodon_maude",
        "desc": "test that all messages are received on isolated mastodon channel",
        "expected": "false",
        "build_cfg": "only_mastodon", 
        "arg": "all-msgs-received(initConfig) and non-zero-msgs-received(initConfig)"
    }
]