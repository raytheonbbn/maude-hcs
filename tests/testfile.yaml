
network:
  graph:
    type: gml
    file:
      path: "./testnetfile.gml" 
      compression: null

hosts:
  A:
    network_node_id: 0
    processes:
    - path: "BB:"
      args: "daemon -c /a/b.conf"
 
  hostB:
    network_node_id: 1
    processes:
    - path: "BB:"
      args: "daemon -c /a/b.conf"

  hostC:
    network_node_id: 2
    processes:
    - path: "BB:"
      args: "daemon -c /a/b.conf"

  hostD:
    network_node_id: 3
    processes:
    - path: "BB:"
      args: "daemon -c /a/b.conf"
 
  hostE:
    network_node_id: 4
    processes:
    - path: "BB:"
      args: "daemon -c /a/b.conf"

  hostF:
    network_node_id: 5
    processes:
    - path: "application:"
      args: "tail -f /dev/null"
    - path: "."
      args: "mydaemon -f -c -P pass 10.0.0.1 x.y.com"
    - path: "."
      args: "python3 server.py -l data/logs/ -o data/output/"

  hostG:
    network_node_id: 6
    processes:
    - path: "application:"
      args: "tail -f /dev/null"
    - path: "."
      args: "client -f -M 255 -T XXX -r -L0 -P pass x.y.com"
    - path: "."
      args: "python3 client.py -f large.dat -l data/logs/ -c 1 -a medium.yaml -m 1000 -s 100"

  perf:
    network_node_id: 7
    processes:
    - path: "perf:"
      args: "tail -f /dev/null"
    - path: "."
      args: "./perf_profiles.sh low 10"